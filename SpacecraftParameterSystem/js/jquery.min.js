/*
 * jQuery v3.6.0
 * 
 * DOWNLOAD REQUIRED:
 * Please download jQuery 3.6.0 from:
 * https://code.jquery.com/jquery-3.6.0.min.js
 * 
 * Save this file as jquery.min.js in the WebContent/js/ directory
 * 
 * Alternative: You can use CDN by replacing the script in index.jsp with:
 * <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
 */

// Placeholder jQuery - Replace with actual jQuery library
console.log("Please download and replace with actual jQuery library");

// Basic jQuery-like functionality for testing
window.$ = window.jQuery = function(selector) {
    if (typeof selector === 'function') {
        // Document ready
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', selector);
        } else {
            selector();
        }
        return;
    }
    
    return {
        ready: function(callback) {
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', callback);
            } else {
                callback();
            }
        },
        val: function(value) {
            const elem = document.querySelector(selector);
            if (value !== undefined) {
                elem.value = value;
                return this;
            }
            return elem ? elem.value : '';
        },
        text: function(text) {
            const elem = document.querySelector(selector);
            if (text !== undefined) {
                elem.textContent = text;
                return this;
            }
            return elem ? elem.textContent : '';
        },
        html: function(html) {
            const elem = document.querySelector(selector);
            if (html !== undefined) {
                elem.innerHTML = html;
                return this;
            }
            return elem ? elem.innerHTML : '';
        },
        hide: function() {
            const elem = document.querySelector(selector);
            if (elem) elem.style.display = 'none';
            return this;
        },
        show: function() {
            const elem = document.querySelector(selector);
            if (elem) elem.style.display = 'block';
            return this;
        },
        on: function(event, callback) {
            const elem = document.querySelector(selector);
            if (elem) elem.addEventListener(event, callback);
            return this;
        }
    };
};
